# MeeSai V2 — Customer Journey Walkthrough Audit
### วันที่ตรวจ: 10 กุมภาพันธ์ 2026 (อัปเดต 22:26)
### ตรวจโดย: ทีมพัฒนา (Automated Code Audit)

> **สรุปผล:** ✅ ผ่าน 19 ข้อ | ⚠️ ทำได้บางส่วน 4 ข้อ | ❌ ยังไม่มี 8 ข้อ

---

## 🗺️ Journey 1: The Renter (ผู้เช่า)

### Phase 1: Discovery & Search

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| 1.1 | **Date Picker** บน Landing/Browse | ✅ | `DateRangeFilter` component ทำงานได้ — ผู้ใช้เลือกวัน pickup/return ก่อน browse |
| 1.2 | **กรองชุดไม่ว่างออก** เมื่อเลือกวันแล้ว | ✅ | Browse page ใช้ overlap detection: `pickupDate ≤ requestReturn AND bufferEnd ≥ requestPickup` — ชุดที่ติดจองจะแยกเป็นกลุ่ม "ຕິດຈອງ" พร้อม greyscale |
| 1.3 | **Date Picker UX บนมือถือ** | ⚠️ | ใช้ `<input type="date">` native — ทำงานได้แต่ UX อาจไม่สวยเท่า custom date picker |
| 1.4 | **รูปภาพโหลดไว** | ✅ | ใช้ `next/image` + lazy loading + responsive sizes ทุกหน้า |
| 1.5 | **ตารางไซส์** ชัดเจน (อก/เอว/สะโพก) | ✅ | Product Detail แสดง bust/waist/hip/height range + body type tags + event theme tags |
| 1.6 | **คำแนะนำรูปร่าง** | ✅ | Body Type matching: PETITE, STANDARD, CURVY, PLUS_SIZE, TALL |

### Phase 2: Booking & Payment

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| 2.1 | **สรุปยอดเงิน** แยกชัดเจน | ✅ | `calculateFees()` คืนค่า: rentalFee, deliveryFee, laundryFee, backupFee, serviceFee, totalAmount, holdAmount (มัดจำ) — Checkout page แสดงแยกทุกรายการ |
| 2.2 | **แยก "ค่าเช่าจริง" vs "มัดจำ (ได้คืน)"** | ✅ | Checkout UI แยก totalAmount กับ holdAmount (deposit) ด้วย style ต่างกัน |
| 2.3 | **คำนวณค่าส่งตามระยะทาง** | ⚠️ | ตอนนี้ค่าส่ง **คงที่ 30,000 ₭** — มี comment `// TODO: dynamic based on distance` |
| 2.4 | **จ่ายผ่าน BCEL One (QR Code)** | ❌ | **ยังไม่เชื่อม Payment Gateway** — ระบบสร้าง booking status `AWAITING_PAYMENT` + deadline 30 นาที แต่ไม่มี payment verification |
| 2.5 | **สถานะเปลี่ยน PENDING → CONFIRMED** | ⚠️ | `createBooking()` สร้างเป็น `AWAITING_PAYMENT` ถูกต้อง — แต่ transition ไป `CONFIRMED` ต้องรอ payment confirmation ซึ่งยังไม่มี auto-verify |
| 2.6 | **Lock Inventory ทันที** (กันจองซ้อน) | ✅ | `createBooking()` ทำ overlap check + `garment.status = RESERVED` ทันที |

### Phase 3: Delivery & Unboxing

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| 3.1 | **แจ้งเตือน 1 วันก่อนส่ง** (SMS/Notification) | ❌ | **ไม่มีระบบ Notification** — ไม่มีโค้ด SMS, LINE, หรือ push notification ใดๆ |
| 3.2 | **สถานะ SHIPPING → DELIVERED** | ✅ | `updateOrderStatus()` ใน partner actions รองรับ — บันทึก `shippedAt` timestamp |

### Phase 4: Usage & Return

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| 4.1 | **ปุ่ม SOS Help** | ✅ | หน้า `/sos` มีครบ 4 สถานการณ์ฉุกเฉิน + ปุ่มโทร + ปุ่มแจ้ง LINE |
| 4.2 | **Pre-Return Self Check** (ถ่ายรูปยืนยัน) | ✅ | หน้า `/account/bookings/[id]/return` มี checklist + upload photos — Server Action `uploadPreReturnPhotos()` บันทึก URLs ลง DB |
| 4.3 | **กรอบแนะนำมุมกล้อง** (Frame) | ❌ | ไม่มี camera frame/guide — เป็นแค่ช่อง upload ธรรมดา |
| 4.4 | **เรียกไรเดอร์รับคืน / Drop-off** | ❌ | ไม่มีระบบเรียก Rider — มีแค่ UI แสดงตัวเลือกวิธีส่งคืน แต่ไม่มี backend integration |

### Phase 5: Refund & Review

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| 5.1 | **ปลดล็อกมัดจำ (QC Pass → คืนเงิน)** | ✅ | Server Action `unlockDeposit()` คืนเงินเข้า Wallet + สร้าง WalletTransaction ครบ |
| 5.2 | **คืนเงินภายใน 24 ชม.** | ⚠️ | Function มีแต่ต้อง **trigger manual** — ไม่มี auto-trigger เมื่อ QC PASS |
| 5.3 | **เขียนรีวิว 5 ดาว** | ❌ | **ไม่มี Server Action สร้างรีวิว** — DB model `Review` มี, Product Detail แสดง reviews ได้, แต่ไม่มี form/action ให้ผู้เช่าเขียนรีวิว |
| 5.4 | **คะแนนรีวิวโชว์ที่หน้าสินค้า** | ✅ | Product Detail page คำนวณค่าเฉลี่ยจาก `review.rating` และแสดง |

---

## 🏪 Journey 2: The Owner/Partner (เจ้าของชุด)

### Phase 1: Order Management

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| P1.1 | **แจ้งเตือนเมื่อมีออเดอร์** (SMS/LINE) | ❌ | **ไม่มีระบบ Notification** — ร้านค้าต้อง login เข้า Dashboard เอง |
| P1.2 | **Dashboard แสดงออเดอร์ใหม่** | ✅ | `getPartnerDashboard()` แสดง toShipCount, qcCheckCount, income (วันนี้/เดือน) |
| P1.3 | **กด CONFIRM รับออเดอร์** | ✅ | `updateOrderStatus(bookingId, "CONFIRMED")` ทำงานได้ |
| P1.4 | **ล็อกวันในปฏิทิน (Buffer)** | ✅ | `bufferEnd` คำนวณจาก `returnDate + bufferDays` อัตโนมัติตอน createBooking |

### Phase 2: Outbound QC

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| P2.1 | **QC ขาออก — ถ่ายรูปสภาพชุด** | ✅ | หน้า `/partner/qc/[bookingId]` มี form ถ่ายรูป photo_before + condition check |
| P2.2 | **รูปผูกกับ Order ID** | ✅ | รูป QC บันทึกใน booking record: `photoBefore`, `photoAfter`, `photoEvidence` |
| P2.3 | **กดปุ่ม SHIP → ลูกค้าได้แจ้งเตือน** | ❌ | กด SHIP ได้ + บันทึก `trackingCode` + `shippedAt` — แต่ **ไม่มีแจ้งเตือนลูกค้า** |

### Phase 3: Inbound QC

| # | Checkpoint | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| P3.1 | **รับคืน + ดึงรูปขาออกมาเทียบ** | ✅ | QC page ดึง `photoBefore` มาแสดงเทียบได้ |
| P3.2 | **Damage Claim (แจ้งความเสียหาย)** | ❌ | DB model `DamageClaim` มี schema ครบ — แต่ **ไม่มี Server Action create/update** และ **ไม่มี UI form** สร้าง claim |
| P3.3 | **กด COMPLETED → เงินเข้า Wallet ร้าน** | ⚠️ | `updateOrderStatus(id, "COMPLETED")` บันทึก `qcPassedAt` — แต่ **ไม่มี auto-transfer เงินเข้า Wallet ร้านค้า** ต้อง manual |

---

## 🚨 Unhappy Path Checklist

| # | สถานการณ์ | สถานะ | หมายเหตุ |
|:--|:--|:--|:--|
| U1 | **Late Return — คิดค่าปรับอัตโนมัติ** | ❌ | ไม่มีโค้ดตรวจ overdue หรือคิดค่าปรับ — schema มี `extensionFee` แต่ไม่มี auto-fine logic |
| U2 | **Cancel Booking — คืนเงินตาม policy** | ✅ | `cancelBooking()` พร้อม Refund Policy 3 ระดับ (100%/>3d, 50%/1-3d, 0%/same day) + ปลดล็อกชุด + คืนมัดจำ + `$transaction` |
| U3 | **Network Fail — กันจองซ้อน** | ✅ | `createBooking()` ใช้ overlap check count ก่อน insert — ป้องกัน double booking ได้ (แต่ยังไม่ใช้ `$transaction` + `SELECT FOR UPDATE` level) |
| U4 | **Extend Rental — เช็คคิวชนคนต่อไป** | ✅ | `requestExtend()` เช็ค overlap กับ booking ถัดไปก่อนอนุมัติ + อัปเดต bufferEnd + จำกัด 1-7 วัน |

---

## 📊 สรุปผลตรวจ

### ภาพรวม

```
     ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ ✅ = 19 ผ่าน
     ⚠️ ⚠️ ⚠️ ⚠️                                     =  4 ทำได้บางส่วน
     ❌ ❌ ❌ ❌ ❌ ❌ ❌ ❌                              =  8 ยังไม่มี
```

### 🔴 Critical (ต้องทำก่อน Launch)

| ลำดับ | รายการ | ความซับซ้อน |
|:--|:--|:--|
| 1 | **Payment Gateway** — เชื่อม BCEL One QR + Auto-verify | สูง |
| ~~2~~ | ~~**Cancel Booking**~~ | ✅ เสร็จแล้ว |
| ~~3~~ | ~~**Extend Rental Overlap Check**~~ | ✅ เสร็จแล้ว |
| 4 | **Notification System** — LINE OA/SMS แจ้งเตือนทุก status change | สูง |

### 🟡 Important (ควรทำก่อน Launch)

| ลำดับ | รายการ | ความซับซ้อน |
|:--|:--|:--|
| 5 | **Damage Claim** — Server Action + UI form สร้าง/จัดการ claim | ปานกลาง |
| 6 | **Review Submission** — Server Action + form ให้ผู้เช่าเขียนรีวิว | ต่ำ |
| 7 | **Auto Settlement** — QC PASS → auto คืน deposit + โอนเงินร้าน | ปานกลาง |
| 8 | **Late Return Auto-Fine** — Cron job ตรวจ overdue + คิดค่าปรับ | ปานกลาง |

### 🟢 Nice to Have (ทำหลัง Launch ได้)

| ลำดับ | รายการ | ความซับซ้อน |
|:--|:--|:--|
| 9 | **Camera Frame Guide** — กรอบแนะนำมุมกล้องตอน Pre-Return | ต่ำ |
| 10 | **Dynamic Delivery Fee** — คำนวณค่าส่งตามระยะทาง | ปานกลาง |
| 11 | **Rider Integration** — ระบบเรียก Rider มารับ/ส่ง | สูง |

---

### ✅ สิ่งที่ทำได้ดี (Strong Points)

1. **Booking Engine** แข็งแกร่ง — Concurrency control, Buffer days, Overlap detection ทำงานถูกต้อง
2. **FSM Status Machine** ครบ 9+ states พร้อม timestamp tracking ทุก transition
3. **Fee Calculation** แยกรายการชัดเจน (rental, delivery, laundry, backup, service, deposit)
4. **Partner Portal** ใช้งานได้จริง — Dashboard + Orders + Inventory + QC Station
5. **Wallet System** ครบวงจร — เติม/ถอน/ล็อกมัดจำ/ปลดล็อก พร้อม audit trail
6. **Pre-Return Self Check** — UI + Photo upload + Server Action ทำงานจบ
7. **SOS Help** — 4 สถานการณ์ฉุกเฉินพร้อมปุ่มโทรด่วน
8. **Cancel Booking** — Refund Policy 3 ระดับ + ปลดล็อกชุด + คืนมัดจำ ครบจบใน `$transaction`
9. **Extend Rental** — เช็คคิวชนก่อนเลื่อน + อัปเดต bufferEnd + จำกัด 1-7 วัน

---

## 🧪 Final Check — Scenario-Based Testing

> เช็คลิสต์สำหรับ QA ก่อน Launch จริง — ทดสอบทุก Edge Case ที่สำคัญ

---

### 🛑 Zone A: The "Change of Heart" (ทดสอบระบบเปลี่ยนใจ/ยกเลิก)

*โจทย์: ระบบต้องจัดการเรื่องเงินและสต็อกได้ถูกต้องแม่นยำ*

**Scenario 1: ยกเลิกก่อนงานเริ่มนานๆ (Early Bird Cancel)**
- [ ] จองงานวันที่ 20 (วันนี้วันที่ 10) → กดจ่ายเงินแล้ว
- [ ] กดปุ่ม "ยกเลิกการจอง"
- [ ] **Expected:** สถานะ → `CANCELLED` + เงินคืน Wallet **100%** + ชุด → `AVAILABLE` ทันที

**Scenario 2: ยกเลิกกะทันหัน (Last Minute Cancel)**
- [ ] จองงานพรุ่งนี้ → กดจ่ายเงินแล้ว
- [ ] กดปุ่ม "ยกเลิกการจอง"
- [ ] **Expected:** สถานะ → `CANCELLED` + เงินคืน Wallet **50%** + แจ้ง User ว่าโดนหักค่าเสียโอกาส

**Scenario 3: ไม่จ่ายเงิน (Payment Timeout)**
- [ ] กดจอง → ทิ้งหน้าจอ QR Code ไว้เกิน 30 นาที
- [ ] **Expected:** ออเดอร์ตัดเป็น `CANCELLED` อัตโนมัติ + ชุดหลุดจอง → `AVAILABLE`

---

### 🔄 Zone B: The "Extension" (ทดสอบระบบขอเช่าต่อ)

*โจทย์: ต้องไม่ให้คิวชนกัน (Overlap)*

**Scenario 4: ขอต่อเวลา (Happy Path)**
- [ ] สถานะเป็น `IN_USE` (กำหนดคืนพรุ่งนี้) → ไม่มีใครจองต่อ
- [ ] กด "ขยายเวลาเช่า" + เลือก 1 วัน
- [ ] **Expected:** คำนวณค่าเช่าเพิ่ม (30%/วัน) + อัปเดตวันคืนใหม่ + อัปเดต `bufferEnd`

**Scenario 5: ขอต่อเวลาแต่มีคิวชน (Conflict)**
- [ ] ให้ User B จองชุดเดียวกันต่อจาก User A ไว้แล้ว
- [ ] User A กด "ขยายเวลาเช่า"
- [ ] **Expected:** ระบบ **ปฏิเสธ (Reject)** + แจ้ง `"ບໍ່ສາມາດຂະຫຍາຍໄດ້ — ຊຸດນີ້ມີຄິວຈອງຕໍ່ໄປແລ້ວ ກະລຸນາຄືນຕາມກຳໜົດ"`

---

### 📦 Zone C: The "Logistics & QC" (ทดสอบระบบรับส่ง + ตรวจคุณภาพ)

*โจทย์: หน้างานจริงกับการเปลี่ยนสถานะ*

**Scenario 6: การส่งคืนปกติ (Return Flow)**
- [ ] User กด "แจ้งคืนชุด" → อัปโหลดรูปสภาพชุด
- [ ] Partner กด QC → ตรวจสภาพดี → กด "ผ่าน QC"
- [ ] **Expected:** สถานะ → `COMPLETED` + คืนมัดจำเต็มจำนวนเข้า Wallet + บันทึก `qcPassedAt`

**Scenario 7: เสียหายและหักเงิน (Damage Claim)**
- [ ] Partner กด QC → ระบุ "ชายกระโปรงขาด" → ใส่ยอดหัก 50,000 ₭
- [ ] **Expected:** คืนมัดจำ = (ยอดเต็ม − 50,000 ₭) → สร้าง `DamageClaim` record → สถานะ → `COMPLETED`

---

*ตรวจสอบจากโค้ดจริง ณ วันที่ 10 กุมภาพันธ์ 2026*
*MeeSai V2 — version `2.0.0-alpha.0`*

